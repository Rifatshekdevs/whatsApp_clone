import 'package:flutter/material.dart';

class TextUtils {
  String fontFamily = "SFPro";

  Text normal8(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 8, fontFamily: fontFamily, fontWeight: FontWeight.w500),
    );
  }

  Text normal9(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 9, fontFamily: fontFamily, fontWeight: FontWeight.w500),
    );
  }

  Text normal10(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 10, fontFamily: fontFamily, fontWeight: FontWeight.w500),
    );
  }

  Text bold12(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 12, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal12(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 12, fontFamily: fontFamily, fontWeight: FontWeight.w500),
    );
  }

  Text normal13(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 13, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold13(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 13, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal14(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 14, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold14(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 14, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal15(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 15, fontFamily: fontFamily, fontWeight: FontWeight.w500),
    );
  }

  Text bold15(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 15, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal16(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 16, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold16(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 16, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal18(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 18, fontFamily: fontFamily),
    );
  }

  Text bold18(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 18, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal20(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 20, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold20(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 20, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal22(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 22, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold22(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 22, fontWeight: FontWeight.w700, fontFamily: fontFamily),
    );
  }

  Text normal24(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(color: color, fontSize: 24, fontWeight: FontWeight.w500, fontFamily: fontFamily),
    );
  }

  Text bold24(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 24, fontWeight: FontWeight.w700, fontFamily: fontFamily, color: color),
    );
  }

  Text normal26(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 26, fontWeight: FontWeight.w500, fontFamily: fontFamily, color: color),
    );
  }

  Text bold26(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 26, fontWeight: FontWeight.w700, fontFamily: fontFamily, color: color),
    );
  }

  Text normal28(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 28, fontWeight: FontWeight.w500, fontFamily: fontFamily, color: color),
    );
  }

  Text bold28(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 28, fontWeight: FontWeight.w700, fontFamily: fontFamily, color: color),
    );
  }

  Text normal30(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.w500, fontFamily: fontFamily, color: color),
    );
  }

  Text bold30(text, color, align) {
    return Text(
      text,
      textAlign: align,
      style: TextStyle(fontSize: 30, fontWeight: FontWeight.w700, fontFamily: fontFamily, color: color),
    );
  }
}
